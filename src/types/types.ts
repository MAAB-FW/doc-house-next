export type userInput = {
    name: string;
    username: string;
    email: string;
    password: string;
};

export type TReviewCard={
    name: string;
    profession: string;
    review: string;
    avatar: string;
}